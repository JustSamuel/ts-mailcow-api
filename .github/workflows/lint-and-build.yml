name: Lint and build

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build-and-lint:
    uses: GEWIS/actions/.github/workflows/lint-and-build-yarn.yml@v1.15.1
    with:
      node-version: "22.x"
      lint: true
      format: true
      build: true
      test: true
